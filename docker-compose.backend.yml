services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    container_name: club_universal_backend
    env_file:
      - ./backend/.env
    ports:
      - "3000:3000"
    restart: unless-stopped
